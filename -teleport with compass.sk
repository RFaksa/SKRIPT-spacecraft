command /party [<text>] [<text>]:
  trigger:
    if arg-1 is not set:
      send "party join"
      send "party join"
      send "party join"
      send "party join"
    if arg-1 is "invite"
      if {party::%player%} is "leader"
      #invite players arg-2
  
